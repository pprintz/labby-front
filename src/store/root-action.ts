import { routerActions } from 'connected-react-router';
import * as charactersActions from '../features/characters/actions';

export default {
  router: routerActions,
  characters: charactersActions,
};
